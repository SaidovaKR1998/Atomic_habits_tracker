# Atomic Habits Tracker 🏆

Курсовая работа - бэкенд часть SPA веб-приложения для трекера полезных привычек, вдохновленного книгой Джеймса Клира "Атомные привычки".

## 🚀 Быстрый запуск

### Вариант 1: Локально с Docker Compose (рекомендуется)

# 1. Клонируйте репозиторий
git clone https://github.com/SadovaKR1998/Atomic_habits_tracker.git
cd Atomic_habits_tracker

# 2. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env (задайте SECRET_KEY, TELEGRAM_BOT_TOKEN и т.д.)

# 3. Запустите все сервисы
docker-compose up -d --build

# 4. Проверьте статус
docker-compose ps

### Вариант 2: Без Docker

pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

### 📋 Функциональность

- ✅ Регистрация и аутентификация пользователей (JWT)
- ✅ CRUD операции для привычек
- ✅ Валидации привычек согласно требованиям книги
- ✅ Пагинация (5 привычек на страницу)
- ✅ Публичные привычки
- ✅ Настройка CORS для фронтенда
- ✅ Интеграция с Telegram для напоминаний
- ✅ Отложенные задачи через Celery
- ✅ Полное покрытие тестами (>80%)
- ✅ Проверка кода Flake8
- ✅ CI/CD с GitHub Actions 
- ✅ Автоматический деплой на сервер

### 🐳 Docker Compose сервисы

После запуска docker-compose up -d будут доступны:

Сервис	        Порт	Назначение
django_backend	8000	Django приложение с API
postgres_db	    5432	PostgreSQL база данных
redis_cache	    6379	Redis для кеша и Celery
celery_worker	 -	    Фоновые задачи (напоминания)
celery_beat	     -	    Планировщик периодических задач

📌 Проверка работы: docker-compose ps (должно быть 5 контейнеров в статусе "Up")

### 🔧 CI/CD Pipeline (GitHub Actions)
Проект использует автоматизированный пайплайн:

## 📌 Триггеры:

- Пуш в ветки main или develop → тесты + деплой (только для main)
- Pull Request в main → только тесты

## 📌 Этапы CI/CD:

1. Тестирование (test job):
- Установка зависимостей 
- Запуск миграций 
- Запуск тестов Django 
- Проверка Flake8

2. Деплой (deploy job):
- Подключение к серверу по SSH 
- Остановка старых контейнеров 
- Сборка новых Docker образов 
- Запуск миграций 
- Перезапуск приложения

## 📌 Настройка автоматического деплоя:

1. Добавьте секреты в GitHub (Settings → Secrets → Actions):

SERVER_HOST: 158.160.89.10 (IP вашего сервера)
SERVER_USER: skr_1998 (пользователь сервера)
SSH_PRIVATE_KEY: ваш приватный SSH ключ

2. При пуше в main → автоматический деплой на сервер

### 🌐 Деплой на продакшен сервер

Требования к серверу:
- Ubuntu 20.04+/Debian 
- Docker и Docker Compose 
- Открытые порты: 22 (SSH), 8000 (приложение)

Настройка сервера:
- Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
- Установка Docker Compose
sudo apt install -y docker-compose
- Настройка пользователя
sudo usermod -aG docker $USER

Проверка деплоя:
После успешного деплоя приложение доступно по:
- Health Check: http://ВАШ_IP:8000/api/health/ 
- Swagger UI: http://ВАШ_IP:8000/swagger/

### 📚 API Документация
После запуска сервера доступна документация:
- Swagger UI: http://localhost:8000/swagger/
- ReDoc: http://localhost:8000/redoc/

### 📊 Основные эндпоинты

- POST /api/users/register/ - Регистрация 
- POST /api/token/ - Получение JWT токена 
- GET /api/habits/ - Список привычек пользователя 
- POST /api/habits/ - Создание привычки 
- GET /api/habits/public/ - Публичные привычки
- GET /api/health/ - Health check (проверка работы API)

### 🧪 Тестирование

Локальный запуск тестов:
- Через Docker
docker-compose exec django_backend python manage.py test
- Или напрямую
python manage.py test

Автоматическое тестирование:
- Тесты запускаются автоматически при каждом PR 
- Покрытие тестами >80% 
- Проверка качества кода Flake8

### 📁 Структура проекта

Atomic_habits_tracker/
├── .github/workflows/main.yml          # CI/CD конфигурация
├── docker-compose.yaml                 # Локальный Docker Compose
├── docker-compose.prod.yaml            # Продакшен Docker Compose
├── Dockerfile                         # Docker образ Django
├── .env.example                       # Шаблон переменных окружения
├── requirements.txt                   # Зависимости Python
├── manage.py                         # Django manage
├── config/                           # Настройки Django
├── habits/                           # Основное приложение
└── README.md                         # Эта документация

### 🤝 Участие в разработке

1. Форкните репозиторий 
2. Создайте ветку для фичи (git checkout -b feature/amazing-feature)
3. Сделайте изменения и тесты 
4. Запушите ветку (git push origin feature/amazing-feature)
5. Создайте Pull Request
